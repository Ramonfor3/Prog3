@model IEnumerable<P.Proyect.Models.NOMINAS>

@{
    ViewBag.Title = "Index";
}

<h2>Nomina</h2>

<p>
    @Html.ActionLink("Añadir", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Año)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Monto_Total)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Año)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Monto_Total)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Id_Nomina }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.Id_Nomina }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.Id_Nomina })
        </td>
    </tr>
}

</table>
